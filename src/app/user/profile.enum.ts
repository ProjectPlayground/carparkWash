
export type Profile = 'client' | 'manager' | 'cleaner' | 'admin';

export const ProfileEnum = {
  client: 'client' as Profile,
  manager: 'manager' as Profile,
  cleaner: 'cleaner' as Profile,
  admin: 'admin' as Profile,
};
